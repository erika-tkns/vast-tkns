<h1>キューポイント一覧</h1>

<%= link_to '登録', new_cuepoint_path %>

<ul>
  <% @cuepoints.each do |cuepoint| %>
    <li><%= link_to cuepoint.id, edit_cuepoint_path(cuepoint) %> : <%= cuepoint.name %></li>
  <% end %>
</ul>

